---
name: botong
description: Run a two-role worker/reviewer loop that implements, verifies, independently reviews, and reconciles code changes before returning them to the user. Activate only when the user explicitly invokes Botong for a programming task.
disable-model-invocation: true
argument-hint: "<programming task>"
---

# Botong

Read [the shared Botong workflow](../../shared/botong.md) completely and follow it. Resolve its relative links from the shared file's directory.
